This is a program to balance load over two ISP.

You will need three NICs, one for internal network and other two for ISPs. Refer to requirment.txt for more info.

This program should run as root.

Before running app, update variable in define_variable.sh.
You can get those variable using ip route.

After that run main.py and follow up.